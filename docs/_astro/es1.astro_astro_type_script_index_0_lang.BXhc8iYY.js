import{a as e}from"./lab_main.BHl-4nac.js";import"./konsole.1Z1AgOxG.js";const t=`/*
	Usando lo scope statico definisci una funzione contatore(x) che preso in input x,
	incrementa il contatore e ne restituisce il valore. Il contatore è inizializzato a 0. 
	Puoi usare una variabile globale se necessario.
	Utilizza i breakpoint per visualizzare i frame durante l'esecuzione.
*/

function contatore(x){
  return;
}

`,o=`
_test(contatore, 1, 1)
_test(contatore, 2, 1)
_test(contatore, 4, 2)
`;e(t,o);
