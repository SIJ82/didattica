import{a as n}from"./lab_main.BHl-4nac.js";import"./konsole.1Z1AgOxG.js";const s=`function fib(sn){
    if(n<=1){
        const res = 1
        return res;
    }else{
        bp;
        const left = fib(n-1);
        bp;
        const right = fib(n-2);
        bp;
        const res = left + right;
        return res;
    }
}

bp;
const res = fib(4);
bp;
console.log(res);`;n(s);
