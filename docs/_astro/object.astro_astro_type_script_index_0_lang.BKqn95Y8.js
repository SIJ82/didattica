import{m as n}from"./babel_test.BCX0R-xq.js";const s=`function fib(sn){
    if(n<=1){
        const res = 1
        return res;
    }else{
        bp;
        const left = fib(n-1);
        bp;
        const right = fib(n-2);
        bp;
        const res = left + right;
        return res;
    }
}

bp;
const res = fib(4);
bp;
console.log(res);`;n(s);
