// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://sij82.github.io',
    base: '/didattica', // sostituisci con il nome del tuo repo
    output: 'static'
});
